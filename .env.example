# Copy this file to .env and fill in real values before running docker compose.
#   cp .env.example .env

# Public IP/hostname of the server running the stack.
# Frontends are built with API base http://<SERVER_IP>:8080.
# Use "localhost" to test the full stack on your own machine.
SERVER_IP=164.68.120.133

MYSQL_ROOT_PASSWORD=change-me
